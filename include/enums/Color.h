#pragma once

/**
 * Represents the two colors (sides) in chess.
 * WHITE moves first and plays from ranks 1-2.
 * BLACK moves second and plays from ranks 7-8.
 */
enum class Color {
    WHITE,
    BLACK
};
